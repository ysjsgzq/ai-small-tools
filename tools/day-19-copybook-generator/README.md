# Day 19 · 字帖生成器

整页按 A4 实际尺寸（210×297mm）所见即所得排版：屏幕上什么样，打印出来就什么样。字多自动分页，每页完整 A4。浏览器本地生成，不联网、不上传。

## 功能

- **田字格生字**：输入任意汉字（自动去重可选），「每字一组」版式——1 个深色示范格 + N 个浅灰描红格 + M 个空格自写格（描红数、空格数可调），与市售字帖一致；也支持连排模式。格宽（10–20mm）、行距毫米级可调，每行组数按纸宽自动计算。
- **拼音四线三格**：内置声母表（23）/ 韵母表（24）/ 整体认读音节（16），也可输入自定义拼音串；字母按教学规范占格。
- **字母 / 数字**：大写、小写、大小写成对（Aa…Zz）、数字 0-9，一键填充。
- **A4 所见即所得**：纸张按 210×297mm 排版，mm 级参数；分页算法按可用高度自动切页，每页带页码；打印时选「100% 缩放」，格线颜色随打印输出（`print-color-adjust: exact`）。

## 占格规则（拼音教学规范）

| 类别 | 字母 | 本工具实测（格高 26px，线 0/26/52/78） |
| --- | --- | --- |
| 上中格 | b p f d t l h k ü | b 墨迹 16~50，越过第 2 线入上格 ✅ |
| 中格 | a o e i u c m n r s v w x z | a 30~50、m 29~50，落在中格 ✅ |
| 中下格 | g q y | g 底 57、q 底 57，越过第 3 线入下格 ✅ |
| 上中下三格 | j | j 19~61，三格贯穿 ✅ |

## 使用

单文件静态页面，无依赖、无网络请求。在线版：<https://ysjsgzq.github.io/ai-small-tools/tools/day-19-copybook-generator/>

```bash
python3 -m http.server 4173   # 或直接双击 index.html
```

## 实测记录（2026-09-02 v4 重构后，15/15 全部通过）

| 用例 | 要点 | 结果 |
| --- | --- | --- |
| T1 | 纸张物理宽度精确 210mm、高 ≥296.5mm | ✅ |
| T2/T3 | 田字格 14mm；每组结构 1 示范 + 3 描红 + 2 空格 | ✅ |
| T4 | 每行组数自动 = 2（186mm 可用宽 / 88mm 组宽） | ✅ |
| T5/T6 | 10 字 1 页；60 字不去重 30 行 → 3 页（每页 14 行） | ✅ |
| T7 | 格宽改 18mm 后每行组数自动降为 1 | ✅ |
| T8 | baseline 精确第 3 线（±0.4mm）；b/a/m/g/q/j/ü 四类占格达标（mm 基准） | ✅ |
| T9 | 声母 23 项 + 纸张仍 210mm | ✅ |
| T10 | print 媒体：纸张可见、控件隐藏 | ✅ |

## 踩坑记录（真实发生）

-1. **v4 的 UI 又被毙了一次**（「UI 还是不行，看着就觉得不靠谱」）：原生表单控件（浏览器默认的 number/radio）平铺 = 没打磨感，纸张也不够突出。v5 UI 层重做：顶栏+左侧参数面板+右侧纸张预览的双栏布局，控件全部定制（分段选择、滑块、stepper、toggle 开关、描红灰度色板四档），纸张加「适应屏幕/100%/75%」缩放；功能回归全部通过（纸张仍精确 210mm、拼音四类占格达标、声母 23、色板与 stepper 联动）。

0. **v3 整个被号主毙掉**：「太小了，人家一张 A4，这么点怎么打印？」——v3 用网页思维（px 流式布局，打印时才展开），屏幕上是一小条，用户无法判断打印效果。v4 推倒重来：整页按 210×297mm 排版、mm 级参数、所见即所得的纸张预览、自动分页；田字格版式从「每行首字示范」改为市售字帖的「每字一组：1 示范 + N 描红 + M 空格」。

1. **v1 字母全瘫在中格**：四个字母渲染位置完全相同——b 没升上格、g 没降下格、j 没贯穿三格。拼音教学里占格错误是家长一眼能识破的硬伤。
2. **v2 按 CSS 行盒推算 baseline，量出来落在 62 而不是第 3 线 52**：行盒的半行距计算依赖字体内部度量，解析算不准。v3 改为渲染后插入零宽探针实测 baseline，再整体平移到第 3 线——对任意格高都成立。
3. **测试期望值又数错了**：断言「12 个字每行 12 格应为 1 行」，实际输入是 13 个字（人肉数数三连错，前两次见 Day 17）。工具全对，期望值修正后通过。
4. **`page-break-inside` 在现代 Chrome 里被序列化为 `break-inside`**，测试脚本按旧属性名匹配失败——按序列化实际值断言。

## 边界说明

- 控笔练习（线条图形训练）不在范围内——那需要美术设计，不是排版能解决的，如实不做。
- 汉字用系统楷体栈（Kaiti SC / STKaiti / KaiTi），个别系统无楷体时回退衬线体。
- 若浏览器打印预览里格线消失，请在打印设置勾选「背景图形」。
